# todo/заметки

Кастомные типы? Создать специальный кастомный тип,
который будет описывать класс модели поведения способности (уникальный тип) - ValueObject

Реализовать локализацию проекта en, ru (на будущее)

Soft Delete (Archive) карточных колод и карт (помещаем в архив - можно восстановить)

SymphonyProfiler Pack (Отличный инструмент дял профилирования запросов)

Добавить новую расу "Орден белого замка"

Для карт необходимо добавить уникальный вендер код (уникальный индекс)

Механизм фикстур необходимо обновить на вариант с обновлением по вендер коду
php bin/console doctrine:migrations:diff --formatted

Постройка может создать "Дым" на поле